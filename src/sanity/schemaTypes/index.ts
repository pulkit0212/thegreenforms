import type { SchemaTypeDefinition } from "sanity";
import project from "./project";

/**
 * Sanity Schema Types
 * ───────────────────
 * Register all document schemas here.
 */
export const schemaTypes: SchemaTypeDefinition[] = [project];
